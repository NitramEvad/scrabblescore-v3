# Don't list directory contents
Options -Indexes

# Defense in depth: never serve the config files even if PHP handling breaks.
<FilesMatch "^config.*\.php$">
    Require all denied
</FilesMatch>

# The word list is read by words.php — it is not a bulk download.
<FilesMatch "\.txt$">
    Require all denied
</FilesMatch>
